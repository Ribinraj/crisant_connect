import 'package:flutter/material.dart';

class Appcolors {
 

static const kprimarycolor = Color.fromARGB(255, 243, 122, 101);
static const kprimaryLightColor = Color.fromARGB(255, 255, 190, 175);

// Secondary (soft peach-pink matching coral)
static const ksecondarycolor = Color(0xFFFF9E80);

// Border color (light warm coral tone)
static const kbordercolor = Color.fromARGB(255, 245, 180, 165);

// Optional extra colors
static const kbackgroundcolor = Color(0xFFFFF6F3);
static const kcardcolor = Color(0xFFFFEFEA);
static const ktextdark = Color(0xFF5A3A33);
static const ktextlight = Color(0xFF9E7B73);
  static const ktextgreycolor = Color.fromARGB(255, 88, 88, 88);
 
  static const kblackcolor = Colors.black;
  static const kwhitecolor = Colors.white;
  static const kgreencolor = Colors.green;
  static const kredcolor = Colors.red;
  static const kgreyColor = Colors.grey;

}
